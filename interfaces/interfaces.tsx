export interface IConfig {
    countStars: number,
    distance: number
}

export interface IStar {
    value: number,
    x: number,
    y: number
}

export interface IScore {
    realScore: number,
    maxScore: number
}